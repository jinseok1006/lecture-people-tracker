{
  "name": "lecture_track",
  "version": "1.0.0",
  "description": "",
  "main": "src/bin.ts",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "upload": "scp -r src .env  pack*.json jinseok@${AWS_DEST}:tracking"
  },
  "dependencies": {
    "cheerio": "^1.0.0-rc.12",
    "dotenv": "^16.3.1",
    "nodemailer": "^6.9.4"
  },
  "devDependencies": {
    "@types/nodemailer": "^6.4.9"
  }
}
